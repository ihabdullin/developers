{
  "name": "personal-amo-referrer-widget",
  "version": "1.0.0",
  "private": true,
  "description": "Development tests for the existing personal amoCRM integration widget",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "lint": "node --check src/core.js && node --check src/api.js && node --check src/card.js && node --check src/widget.js && node --check script.js",
    "build": "python3 build.py"
  },
  "devDependencies": {
    "jquery": "3.7.1",
    "jsdom": "26.1.0"
  }
}
